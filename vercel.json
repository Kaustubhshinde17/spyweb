{
    "buildCommand": "cd spyweb-digital-fortress && npm install && npm run build",
    "outputDirectory": "spyweb-digital-fortress/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}